def force(p, x, y):
    fx = 0.0
    fy = 0.0
    
    for i in range(len(x) - 1):
        fx += float((p[i] + p[i+1]) / 2.0) * float(y[i+1] - y[i])
        fy += -float((p[i] + p[i+1]) / 2.0) * float(x[i+1] - x[i])
        
    return float(fx), float(fy)
